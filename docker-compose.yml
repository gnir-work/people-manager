version: "3.7"
services:
    mongo:
        image: "mongo:latest"
        ports: 
            - "27017:27017"
    frontend:
        build: frontend
        ports:
            - "80:80"
    backend:
        build: backend
        ports: 
            - "8000:8000"